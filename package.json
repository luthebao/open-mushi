{
  "scripts": {
    "fmt:check": "echo 'TODO'",
    "fmt": "dprint fmt",
    "lint": "oxlint --type-aware || eslint",
    "tauri:dev:env": "set -a; . ./.env; set +a; pnpm turbo tauri:dev --filter=@openmushi/desktop",
    "tauri:build:env": "set -a; . ./.env; set +a; if [ -n \"$TAURI_SIGNING_PRIVATE_KEY_PATH\" ] && [ -z \"$TAURI_SIGNING_PRIVATE_KEY\" ]; then export TAURI_SIGNING_PRIVATE_KEY=\"$(cat \"$TAURI_SIGNING_PRIVATE_KEY_PATH\")\"; fi; pnpm turbo tauri:build --filter=@openmushi/desktop"
  },
  "devDependencies": {
    "@tanstack/eslint-plugin-query": "^5.91.4",
    "dprint": "^0.51.1",
    "eslint": "^9.39.2",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "oxlint-tsgolint": "^0.12.2",
    "turbo": "2.8.3",
    "typescript-eslint": "^8.55.0"
  },
  "packageManager": "pnpm@10.30.0",
  "engines": {
    "node": ">=22"
  }
}
