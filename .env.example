# Required by google-calendar plugin at Rust compile time
VITE_API_URL="http://localhost:3001"

# Required for tauri updater artifact signing during tauri:build
TAURI_SIGNING_PRIVATE_KEY_PATH="/absolute/path/to/tauri_signing.key"
TAURI_SIGNING_PRIVATE_KEY_PASSWORD="your-key-password"
